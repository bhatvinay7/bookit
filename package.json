{
  "name": "bookit",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "seed:searchable": "node scripts/seed_searchable_shows.js",
    "seed:movie-data": "node scripts/update_movie_data.js",
    "seed:schedules:two-weeks": "node scripts/ensure_two_week_schedules.js"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "ioredis": "^6.0.0",
    "mongodb": "^7.5.0",
    "pg": "^8.22.0"
  }
}
