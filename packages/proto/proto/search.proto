syntax = "proto3";

package search;

service SearchService {
    rpc Search (SearchRequest) returns (SearchResponse);
}

message SearchRequest {
    string query = 1;
    string city = 2;
}

message SearchResponse {
    // A serialized JSON string of the search results for simplicity in bridging to the frontend
    string results_json = 1;
}
